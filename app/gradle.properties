android.useAndroidX=true
android.enableJetifier=true
# Required for switch (view.getId()) { case R.id.* } — IDs must be compile-time constants.
android.nonFinalResIds=false
android.nonTransitiveRClass=false
