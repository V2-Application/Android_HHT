<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- App-private external Downloads -->
    <external-files-path name="external_downloads" path="Downloads/" />
    <!-- Public Downloads folder — APK update is saved here as V2_HHT_Update.apk -->
    <external-path name="public_downloads" path="Download/" />
    <!-- Fallback for older code paths -->
    <external-path name="external_storage" path="." />
</paths>